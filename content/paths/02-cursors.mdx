import { PageSection } from "./components/PageSection";
import { PathEditor } from "./components/PathEditor";
import { PageProvider } from "./components/PageProvider";
import { PageFooter } from "./components/PageFooter";
import {
  PageWrapper,
  ArticleWrapper,
  GraphWrapper,
} from "./components/PageWrapper";
import { CursorVisual } from "./components/cursors";

<PageWrapper page="02-cursors" numSections={4}>

<ArticleWrapper>

<PageSection index={0}>

# Cursors

All SVG paths revolve around the **cursor**. As commands are executed, the cursor moves around the canvas, drawing a path behind it.

</PageSection>

<PageSection index={1}>

The cursor always starts at the origin `(0, 0)`, the top left corner. The `M`, or **move**, command lets you move the cursor to a new position without drawing a path:

```
M 10 20
```

</PageSection>

<PageSection index={2}>

The lowercase `m` command also moves the cursor, except it moves the cursor relative to its _current_ position. In this example, the final position of the cursor is `(20, 12.5)`:

```
M 10 20
m 10 -7.5
```

</PageSection>

<PageSection index={3}>

## Practice

Using the `M` and/or `m` commands, **move the cursor to the points shown on the canvas**.

<PathEditor />

</PageSection>

<PageFooter />

</ArticleWrapper>

<GraphWrapper>
  <CursorVisual />
</GraphWrapper>

</PageWrapper>
